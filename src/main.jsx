// main.jsx
import React from "react"
import ReactDOM from "react-dom/client"
import FlowHub from "./App.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FlowHub />
  </React.StrictMode>
)